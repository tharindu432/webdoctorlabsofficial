// /** @type {import('next').NextConfig} */
// const nextConfig = {};

import next from 'next';

// export default nextConfig;

/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
};

export default nextConfig;